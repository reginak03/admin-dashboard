import React from "react";
import ReactDOM from "react-dom"; //hook react application to the real dom

import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); //hook react application to the root div